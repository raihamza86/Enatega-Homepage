module.exports = {
    e2e: {
      baseUrl: 'http://localhost:3000', // Replace with your app's URL if necessary
      specPattern: 'cypress/e2e/**/*.spec.js', // Adjust the spec pattern if needed
    },
  };
  